<Window x:Class="FpsOverlayer.OverlayCode.WindowBrowser"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FpsOverlayer.OverlayCode"
        RenderOptions.BitmapScalingMode="HighQuality"
        SnapsToDevicePixels="True"
        mc:Ignorable="d"
        Title="FpsOverlayer Browser"
        Topmost="True"
        WindowStyle="None"
        WindowState="Normal"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        ResizeMode="CanResize"
        Background="Transparent"
        MinHeight="200"
        MinWidth="350"
        Opacity="0.98">

    <Border CornerRadius="5">
        <Border.Background>
            <SolidColorBrush Color="{DynamicResource ApplicationBlackColor}" Opacity="0.98"/>
        </Border.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Name="grid_Browser" Margin="5"/>
            <Grid Grid.Row="1" Name="grid_Bottom" Margin="5,0,5,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Name="textbox_WebUrl" Style="{StaticResource TextBoxWhite}" KeyUp="textbox_WebUrl_KeyUp"/>
                <StackPanel Grid.Column="1" Margin="5,0,0,0" HorizontalAlignment="Right" Orientation="Horizontal">
                    <Button Name="button_Check" Style="{StaticResource ButtonImageTransparentWhiteBorder}" Click="button_Check_Click" ToolTip="Open link in browser">
                        <Image Source="pack://siteoforigin:,,,/Assets/Default/Icons/Check.png" Height="35"/>
                    </Button>
                    <Button Name="button_Back" Style="{StaticResource ButtonImageTransparentWhiteBorder}" Click="button_Back_Click" ToolTip="Go back to previous page">
                        <Image Source="pack://siteoforigin:,,,/Assets/Default/Icons/MoveLeft.png" Height="35"/>
                    </Button>
                    <Button Name="button_Close" Style="{StaticResource ButtonImageTransparentWhiteBorder}" Click="button_Close_Click" ToolTip="Close the browser">
                        <Image Source="pack://siteoforigin:,,,/Assets/Default/Icons/Close.png" Height="35"/>
                    </Button>
                    <Button Name="button_Pin" Style="{StaticResource ButtonImageTransparentWhiteBorder}" Click="button_Pin_Click" ToolTip="Browser click through mode">
                        <Image Source="pack://siteoforigin:,,,/Assets/Default/Icons/Pin.png" Height="35"/>
                    </Button>
                    <Image Name="button_Move" Cursor="SizeAll" Source="pack://siteoforigin:,,,/Assets/Default/Icons/Move.png" Height="38" MouseDown="button_Move_MouseDown"/>
                    <Thumb Style="{StaticResource ThumbGripWhite}" Width="35" Height="35" DragDelta="ResizeGripper_DragDelta"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>